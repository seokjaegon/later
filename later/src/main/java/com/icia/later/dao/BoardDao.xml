<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.icia.later.dao.BoardDao">
	<insert id="insertBoard" parameterType="board">
		INSERT INTO board ( category, companyName, periodStart, 
							periodEnd, personnel, price, 
							provideType, detail, checkInfo, 
							boardFile, customerId)
		VALUES (#{category}, #{companyName}, #{periodStart}, 
				#{periodEnd}, #{personnel}, #{price}, 
				#{provideType}, #{detail}, #{checkInfo},
				#{boardFile},  #{customerId})
	</insert>
	
	<update id="updateBoard" parameterType="board">
		UPDATE board
		SET periodStart=#{periodStart}, periodEnd=#{periodEnd}, 
		personnel=#{personnel}, detail=#{detail}, 
		checkInfo=#{checkInfo}, price=#{price},
		boardFile=#{boardFile}
		
		WHERE boardId = #{boardId}
	</update>
	
	<select id="selectBoard" parameterType="Integer"
			resultType="board">
			SELECT * FROM board WHERE boardId = #{boardId}
	</select>
	
	<select id="getBoardList" resultType="board">
		SELECT * FROM board
	</select>
	
	<delete id="deleteBoard" parameterType="Integer">
		DELETE FROM board WHERE boardId = #{boardId}
	</delete>
</mapper>